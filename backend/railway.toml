[build]
builder = "docker"

[deploy]
numReplicas = 1
sleepApplication = false
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
ASYNC_SQLALCHEMY = "true"

[volume]
name = "data"
mountPath = "/data"

